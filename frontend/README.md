# Expense Management Frontend

Angular frontend for the expense management application.

## Run Tasks

Start the development server:

```sh
npx nx serve expense-management
```

Create a production bundle:

```sh
npx nx build expense-management
```

Show available project targets:

```sh
npx nx show project expense-management
```